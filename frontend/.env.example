# Override the API base URL when not using the Vite proxy.
# Leave empty/unset in dev — Vite proxies /api → http://localhost:8000 (see vite.config.ts).
# VITE_API_BASE_URL=http://localhost:8000/api/v1
